document.addEventListener('DOMContentLoaded', (event) = {
    const container = document.getElementById('game-container');
    container.innerHTML = 'pThe game will be here!p';

     Add game logic here
});
